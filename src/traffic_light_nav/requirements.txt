# =============================================================================
# traffic_light_nav - Python dependencies (Project B: Traffic Light Obedience)
# =============================================================================
#
# ON THE ROBOT (Husarion ROSbot 3 PRO, ROS 2 Jazzy, Ubuntu 24.04):
#   PREFER system apt packages over pip to avoid ABI mismatches with cv_bridge:
#     sudo apt install -y \
#         ros-jazzy-cv-bridge \
#         ros-jazzy-rclpy \
#         ros-jazzy-sensor-msgs \
#         ros-jazzy-std-msgs \
#         ros-jazzy-geometry-msgs \
#         ros-jazzy-nav-msgs \
#         python3-opencv \
#         python3-numpy
#
# ON A LAPTOP (development, unit testing, no ROS installed):
#     pip install -r requirements.txt
# =============================================================================

# OpenCV: HSV colour segmentation for red/yellow/green signal detection.
# On the robot, prefer the system python3-opencv to avoid cv_bridge conflicts.
opencv-python>=4.6.0

# NumPy: array maths for HSV masking and depth-region statistics.
numpy>=1.21
